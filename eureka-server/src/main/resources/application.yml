server:
  port: 8858
spring:
  profiles:
    active: loc
  application:
    name: eurekaServer
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8888/eureka/